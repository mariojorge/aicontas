# ====================================
# Docker Compose Configuration
# ====================================

# Application port - used by Docker Compose to configure the container
# This port will be used for both internal and external access
APP_PORT=3000

# Node environment (development, production)
NODE_ENV=production

# JWT Secret for authentication - CHANGE THIS IN PRODUCTION!
# Generate a secure secret: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-change-this-in-production

# ====================================
# Development Configuration (Optional)
# ====================================

# Frontend development server port (only for local development without Docker)
VITE_PORT=5173

# Backend API URL for frontend development (only for local development without Docker)
VITE_API_URL=http://localhost:3000

# ====================================
# Notes
# ====================================
# 1. Copy this file to .env and update the values
# 2. The APP_PORT variable controls all port configuration in Docker
# 3. To change the application port, only modify APP_PORT
# 4. The database is stored in a Docker volume at /app/data/finance.db